import { ToggleFunctionDirective } from './toggle-function.directive';

describe('ToggleFunctionDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleFunctionDirective();
    expect(directive).toBeTruthy();
  });
});
